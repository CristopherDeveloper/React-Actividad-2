const SearchNotFound = ({text}) => {
    return(
        <section className="searchnotfound">
            <p>
                {text}
            </p>
        </section>
    )
}

export default SearchNotFound;